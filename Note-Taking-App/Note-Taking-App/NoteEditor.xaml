<Window x:Class="Note_Taking_App.NoteEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Note_Taking_App"
        mc:Ignorable="d"
        Title="NoteEditor" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <TextBox Name = "NoteName" Text = "{Binding Path=NoteNameProp, Mode=TwoWay}" Grid.Row="0"/>

        <Grid Grid.Row="1">
            <TextBox Name ="NoteContent" Text = "{Binding Path=NoteContentProp, Mode=TwoWay}"/>
        </Grid>
 
        
    </Grid>
</Window>
